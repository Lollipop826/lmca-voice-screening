# 长期记忆语义检索与流式语音修改方案

> 状态：待实施
>
> 范围：正式跨会话语义检索、当前会话边界、Agent 上下文、流式 ASR 预取、性能观测与验收。

## 1. 目标状态

```text
患者持续说话
  -> 流式 ASR partial：字幕、可取消预取、即时风险陪伴
  -> 滚动情绪观察

VAD 判定本轮结束
  -> ASR final
  -> 文本有效性检查 + 最终风险门
  -> Memobase search_event_gist(final_text, topk=3)
  -> 组装当前会话短期记忆 + 权威事实 + 相关长期事件 + 当前情绪
  -> qwen-flash 流式生成
  -> 流式 TTS
```

当前会话仅使用短期会话记忆。患者或医护明确结束会话、或连接断开后，后台整理并同步该会话到 Memobase。

## 2. P0：正式长期记忆检索

### 2.1 替换正常语音检索路径

修改 `src/context_management/emotion_memobase.py` 和 `src/voice/services.py`：

1. 正常 final ASR 路径调用 Memobase 结构化 `search_event_gist`。
2. 查询参数固定为 `topk=3`。
3. 删除 `exclude_session_id` 触发 SQLite 最近记录读取的正常路径旁路。
4. 删除“按 SQLite `id` 倒序取最近 12 条并作为相关长期记忆”的正常逻辑。
5. 禁止正式路径使用不透明 `user.context()` 作为长期记忆证据。
6. Memobase 失败、超时或无有效候选时，只保留权威事实卡和当前会话短期记忆；不得把 SQLite 最近记录伪装成语义检索结果。

### 2.2 检索范围与返回结构

调整 Memobase 查询接口和应用层调用：

1. 默认查询同一 `patient_id`、同一 `project_id` 的全部已结束会话。
2. 去除当前默认 21 天范围；时间范围改为可选配置。
3. 只返回通过校准相似度阈值的候选，返回数量为 0 至 3。
4. 按余弦相似度降序排序。
5. 对同一原始事件的重复 gist 去重。
6. 返回或维护以下内部字段：`patient_id`、`project_id`、`session_id`、`turn_id`、`event_gist_id`、`source_turn_id`、`created_at`、`similarity`。
7. 检索日志必须记录命中的 `event_gist_id` 与 `source_turn_id`，但这些 ID 不进入 Agent 自然语言 Prompt。

### 2.3 会话边界

1. 活跃会话的轮次不写入 Memobase。
2. 明确结束或断连后，整理该会话并同步 Memobase。
3. 当前会话上下文由短期会话记忆提供，不从 Memobase 召回。
4. 若未来改成逐轮同步，必须在 Memobase 的数据库查询层按 `session_id` 排除当前会话，并返回来源字段。

## 3. P0：Agent 上下文装配

将 Agent 输入拆分为以下区块：

```text
[confirmed_profile]
[current_session_memory]
[retrieved_long_term_memory]
[current_user_text]
[current_state]
```

修改要求：

1. `retrieved_long_term_memory` 包含 0 至 3 条完整事件摘要。
2. 长期事件只包含摘要正文、创建时间、相似度和内部来源元数据。
3. 当前用户明确表达、最终风险判断和人工确认事实的优先级高于历史事件摘要。
4. 历史事件摘要不能覆盖人工修订后的权威事实卡。
5. 不传入跨会话完整原始聊天记录。
6. 不在 Agent 自然语言 Prompt 中传入任何 ID。

### 3.1 动态 Token 预算

替换旧字符截断逻辑：

1. 移除长期记忆路径中的 `_MAX_PERSISTENT_MEMORY_CHARS = 1800` 和 `_MAX_TURN_MEMORY_CHARS = 1800` 外层硬截断。
2. 不再把 `_MAX_CONTEXT_CHARS = 7000` 作为长期记忆通用上限。
3. 先预留系统提示词、工具定义、安全规则、当前用户文本、当前会话记忆和模型输出。
4. 将剩余 Token 分配给相关长期事件。
5. 事件摘要必须整条进入；放不下则跳过整条，不从中间截断。
6. 在真实 Agent Prompt 形状下测量总输入 Token，再确定总请求安全上限。

## 4. P1：流式 ASR 预取和取消

### 4.1 partial 使用边界

允许：

- 前端实时字幕；
- 基于稳定 ASR 片段的可取消 Memobase 预取；
- 明确风险词的即时、固定安全陪伴；
- 滚动情绪观察的辅助信号。

禁止：

- 写入长期记忆；
- 覆盖 final ASR；
- 作为最终风险记录；
- 直接生成正式 Agent 回复；
- 使用不可取消的预取结果。

### 4.2 预取执行

1. 使用流式 ASR 的稳定片段或最终化标记触发预取，不使用固定字数或字符改动阈值。
2. 每次预取带内存态 `retrieval_request_id`。
3. final ASR 到达后验证预取查询：可复用则复用；语义明显变化则取消旧请求并重新检索。
4. 过期 `retrieval_request_id` 的结果不得进入 final Agent Prompt。
5. 新一轮患者语音开始时，取消上一轮尚未播报完成的 TTS 和无效后续任务。
6. 即时安抚、预取、情绪分析不得中断录音、final ASR、轮次持久化和会话封存。

## 5. P1：性能与索引

### 5.1 保持当前 embedding 模型

保持 `vendor/memobase/src/server/api/config.yaml` 中：

```yaml
embedding_model: qwen3.7-text-embedding
embedding_dim: 1024
```

本轮不切换到 `text-embedding-v4`、`text-embedding-v3`、`text-embedding-v2` 或 `text-embedding-v1`。

### 5.2 连接预热和阶段观测

1. 会话建立时预热 embedding 连接。
2. 不保留未经验证的 `0.8s` 记忆读取硬超时。
3. 为每轮记录：`VAD end`、`ASR final`、`embedding start/end`、`pgvector search start/end`、`prompt assembled`、`qwen-flash first token`、`TTS first audio`。
4. 将查询嵌入耗时、数据库向量搜索耗时和 Agent 首 Token 耗时分别上报。

### 5.3 pgvector 索引与压测

1. 为 `user_event_gists.embedding` 增加并验证 HNSW 或 IVFFlat 向量索引。
2. 分别在 0、1000、10000 及更大事件量下测试查询嵌入、向量搜索和最终首句耗时。
3. 在索引建立前后对比 P50、P95 和最大值。

## 6. P2：检索质量与阈值校准

1. 建立脱敏或合成的跨会话事件检索评测集。
2. 覆盖同义改写、较早历史事件、冲突事实、多事件提问、高风险表达和人工修订事实。
3. 评估 `Recall@1`、`Recall@3`、关键记忆漏检率、无关记忆注入率、当前会话泄漏率。
4. 依据评测集校准相似度阈值；不直接采用 Memobase 默认 `0.2` 作为正式阈值。
5. 对 Top 3 无法覆盖的真实多事件场景单独记录，依据实测结果再决定是否扩展检索策略。

## 7. 验收标准

1. 有效 final ASR 的正常路径调用 `search_event_gist(final_text, topk=3)`。
2. SQLite 最近 12 条不再作为正常长期记忆来源。
3. Agent 接收 0 至 3 条完整、通过阈值的跨会话事件摘要。
4. 当前会话内容不会从 Memobase 检索回来。
5. partial 预取的过期结果不会进入 final Agent Prompt。
6. 高风险 final ASR 即使 Memobase、Agent 或 TTS 失败，仍先执行确定性安全处置。
7. 每轮可从日志定位命中的 gist、来源轮次、是否降级和各阶段耗时。
8. 大体量历史记忆下的检索延迟、Recall@3 和无关记忆注入率均有实测证据。
