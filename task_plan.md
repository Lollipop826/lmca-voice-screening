# 长期记忆规模延迟实验

## 目标

清理旧实验数据，直接构造 S=1000 的可检索长期记忆，完成 embedding/索引验证，并在保留 Emotion2Vec 的条件下对比长期记忆开/关的语音对话延迟。

## 成功标准

- 正式 `.env` 保持 `ENABLE_LONG_TERM_MEMORY=true`。
- 旧实验 SQLite、Memobase 用户数据和向量索引均被清理。
- 1000条规模按可检索的结构化记忆/事件条目统计，不按原始文本行数冒充。
- S=1000 完成 embedding/索引并通过真实命中与未命中查询验证。
- 开/关两组使用同一数据快照、同一音频、同一模型和同一情绪识别配置。
- 每组 1 次预热、至少 30 次正式样本；Emotion2Vec 未实际运行的样本整组失败。
- 输出各档 `asr_to_first_tts_byte_ms`、记忆检索耗时、分位数和开关差值。

## 阶段

| 阶段 | 状态 | 验证 |
|---|---|---|
| 1. 核对现有记忆写入、embedding、检索和基准脚本 | 已完成 | 找到真实调用链与可复用接口 |
| 2. 实现并测试清理、分层数据生成和索引验收工具 | 已完成 | `py_compile` 通过，相关回归 `60 passed` |
| 3. 清理旧实验数据并建立 S=1000 基线 | 已完成 | 1000条计数、embedding、命中结果通过验收 |
| 4. 执行长期记忆开/关对照测试 | 已完成 | 开/关各30个有效样本，Emotion2Vec 60/60通过 |
| 5. 汇总统计与实验边界 | 已完成 | JSON原始结果和中文摘要已生成 |

## 风险与取舍

- 5000 条的 embedding/结构化提取可能产生明显时间和模型调用成本；必须使用项目真实引擎支持的批量入口，不能用伪向量代替。
- 外部 ASR/LLM/TTS 服务若持续不稳定，会保留完整失败证据，不把不完整样本纳入统计。
- 合成记忆用于控制规模和可重复性，结论代表规模延迟，不代表真实患者数据分布。

## 错误记录

| 错误 | 次数 | 处理 |
|---|---:|---|
| 旧数据 `Remove-Item` 被终端安全策略拦截，未执行删除 | 2 | 不再重复，改用显式路径的 .NET File.Delete，数据库复核单独执行 |
| 一次状态核对误读了不存在的 vendor Compose 路径 | 1 | 改为以项目根 `docker-compose.yml` 和实际 `docker compose config` 为准 |
| 隔离 API 首次构建访问 GHCR/Docker Hub 时 EOF | 1 | 复用本机已构建 Memobase 镜像，并只读挂载当前工作区源码 |
| 本地旧镜像未包含当前源码声明的 HNSW 索引 | 1 | 仅在隔离空库创建当前源码同名 HNSW cosine 索引并验收 |
| S0 初始化后缺少 EmotionMemobase 表 | 1 | `init-local` 显式初始化记忆 schema，并增加0条夹具回归 |
| 容器内从 `/benchmark-scripts` 启动夹具时找不到 `memobase_server` | 2 | 显式设 `PYTHONPATH=/app`；夹具改用 Compose 一次性任务容器运行 |
| 在 API 容器内另起夹具进程占用同一 Prometheus 端口 | 1 | 使用 `docker compose run --rm --no-deps` 隔离进程网络命名空间 |
| 一次性夹具容器首次下载 tiktoken 编码遇到 SSL EOF | 1 | 持久化并只读共享已验证的 `/tmp/data-gym-cache` |
| Windows 终端策略阻止后台 `Start-Process`，前台服务被工具超时终止 | 2 | 使用隔离 Compose 的语音容器，映射到 `127.0.0.1:8503` |
| S0正式组因 BigASR 两次连接重置而整组中止 | 1 | 增加有上限的逻辑样本重试，结果保留全部失败证据 |
| Docker Desktop 整体停止导致语音容器退出137 | 1 | Engine恢复后重启隔离容器；中断样本不计入结果 |
