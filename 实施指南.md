# Memobase 集成 + 情绪模型对比实验 - 实施指南

## 当前状态

✅ **已完成**：
1. Memobase 0.0.27 安装成功
2. SQLite 事实源 + 本地 Memobase 异步镜像、outbox 补发与跨会话检索
3. 本地 Memobase、PostgreSQL、Redis 的 Compose 部署配置
4. Emotion2Vec+ 与 DashScope 延迟基准框架、测试集准备工具

⚠️ **待完成**：
1. 准备真实音频测试数据
2. 在已配置的实际环境运行模型、Memobase 和 BigASR 延迟基准
3. 使用有效凭据验证完整对话流程

---

## 一、快速开始：Memobase 集成

### 1.1 本地部署 Memobase（推荐）

使用根目录 `docker-compose.yml` 中随仓库提供的 Memobase API、PostgreSQL 与 Redis 服务。具体变量、启动与健康检查步骤见 [docs/memobase-local.md](docs/memobase-local.md)；不要使用本节旧版本的单容器镜像命令。

### 1.2 验证 Memobase 连接

```powershell
Invoke-RestMethod -Uri http://127.0.0.1:8019/api/v1/healthcheck
```

---

## 二、情绪模型对比实验流程

### 2.1 准备真实音频测试数据

**选项A：使用现有对话录音**

如果你有真实的心理健康对话录音：

```bash
# 1. 将音频文件放入测试目录
mkdir -p tests/emotion_benchmark/testset/audio

# 2. 准备标注文件 annotations.json
# 格式：
# [
#   {"sample_id": "sample_1", "audio_file": "audio1.wav", "true_emotion": "anxiety", "age_group": "elderly"},
#   {"sample_id": "sample_2", "audio_file": "audio2.wav", "true_emotion": "joy", "age_group": "young"},
#   ...
# ]

# 3. 重新生成测试集（使用真实音频）
python scripts/prepare_emotion_testset.py \
    --annotation_file tests/emotion_benchmark/annotations.json \
    --output_dir tests/emotion_benchmark/testset
```

**选项B：使用公开数据集**

推荐中文情绪语音数据集：
- CASIA Chinese Emotional Speech Database
- CHEAVD 2.0（中文情绪音视频数据集）
- AISHELL-Emotion（如果可获取）

下载后按照上述格式准备 annotations.json

**选项C：录制测试音频（最简单）**

```bash
# 准备 10-20 条不同情绪的测试语音
# 可以自己录制或请志愿者录制：

# 焦虑情绪示例文本：
- "我最近总是睡不着，很担心自己的身体"
- "记忆力下降让我很焦虑"
- "我害怕自己得了什么病"

# 快乐情绪示例文本：
- "今天天气很好，心情不错"
- "孙子来看我了，很开心"

# 平静情绪示例文本：
- "我感觉还好，没什么特别的"
- "今天比较平静"

# 保存为 WAV 格式，放入 tests/emotion_benchmark/testset/audio/
```

### 2.2 运行对比实验

```bash
# 运行 Emotion2Vec+ 测试
python scripts/benchmark_emotion_models.py \
    --testset tests/emotion_benchmark/testset \
    --models emotion2vec \
    --output tests/emotion_benchmark/results.json

# 查看结果
cat tests/emotion_benchmark/results.json
```

### 2.3 延迟实测

以下命令只会在显式传入 `--live` 时调用本地模型或外部服务；执行前需要有效的百炼/火山配置、运行中的本地 Memobase 和真实音频。

```powershell
python scripts\benchmark_latency.py --live `
    --audio .\tests\emotion_benchmark\sample.wav `
    --llm-model qwen3.7-flash `
    --memobase-patient-id patient-id `
    --memory-text "我又想起上次那件事" `
    --ark-asr-audio .\tests\emotion_benchmark\sample.wav
```

输出包含 Emotion2Vec 推理、DashScope 首字/完整回复、Memobase 当前文本检索，以及 BigASR 首个暂时文字/最终文字的延迟。未产生暂时文字的 BigASR 轮次会单独计数，不会将最终文字误报为暂时文字。

### 2.4 解读结果

实验会输出：
- **准确率**：7 类情绪分类准确率
- **F1-Score**：处理类别不平衡
- **延迟指标**：P50/P95/P99 延迟（ms）
- **分年龄段准确率**：elderly / middle_aged / young
- **混淆矩阵**：查看哪些情绪容易混淆

**决策标准**：
- 准确率 > 70%：可用
- P95 延迟 < 300ms：实时对话可接受
- 老年人准确率不应低于整体准确率 10%+

---

## 三、Memobase 集成实施步骤

### 3.1 当前实现

`src/context_management/emotion_memobase.py` 已实现 SQLite 本地事实源、Memobase 异步镜像、失败 outbox 与检索回退；`src/voice/handlers/speech_turn_processor.py` 已在最终文字确认后写入和检索记忆。部署使用根目录 `docker-compose.yml`，详细步骤见 [docs/memobase-local.md](docs/memobase-local.md)。

### 3.2 初始化 SQLite 数据库

应用启动时会自动初始化 SQLite 的患者、会话、长期记忆和情绪轨迹表，不需要单独的 `init_sqlite_schema.sql`。

### 3.3 测试集成

```powershell
python -m pytest -q tests\test_memobase_basic.py tests\voice\test_voice_services.py
```

---

## 四、关键设计决策

### 4.1 为什么选择双存储架构？

| 存储 | 作用 | 优势 | 数据内容 |
|------|------|------|---------|
| **SQLite** | 完整病历 | 本地、完整、可追溯、满足医疗合规 | 原始转写、音频路径、情绪向量、时间戳 |
| **Memobase** | AI 上下文 | 智能检索、主题归纳、跨会话记忆 | 患者画像、关键事件、对话摘要 |

**核心流程**：
1. 每轮对话 → 存入 SQLite（完整）+ 缓冲到 Memobase（待处理）
2. 会话结束 → 触发 Memobase flush（提取主题、生成事件）
3. 下次对话 → 从 Memobase 获取精简上下文（而非查询 SQLite 全表）

### 4.2 为什么不直接把 SQLite 数据全删，只用 Memobase？

**不可行原因**：
1. **合规要求**：医疗对话需要完整留存，Memobase 会删除处理后的原始对话
2. **情绪追溯**：MMSE 评估需要完整的 7 维情绪轨迹数据
3. **音频回放**：医生需要能回放任意一次对话的原始音频
4. **数据主权**：本地 SQLite 确保数据不出域（即使 Memobase 本地部署）

### 4.3 适合全年龄段的设计考虑

| 年龄段 | 特点 | 系统适配 |
|--------|------|---------|
| **老年人（AD 筛查）** | 记忆力下降、语速慢、情绪波动 | 情绪轨迹追踪、MMSE 评估、耐心对话节奏 |
| **中年人（压力管理）** | 工作压力、家庭负担、睡眠问题 | 压力源识别、应对策略推荐、情绪疏导 |
| **年轻人（情绪支持）** | 焦虑抑郁、社交压力、自我认同 | 共情陪伴、认知行为引导、资源推荐 |

**Memobase 的作用**：
- Profile Topics 包含：`基本信息`、`睡眠问题`、`记忆力`、`情绪状态`、`社交关系`、`认知功能`、`应对策略`
- 自动归类不同年龄段的常见问题
- 跨会话记忆：上次提到的困扰、尝试过的方法、有效的应对策略

---

## 五、下一步行动清单

### 立即执行（今天）

- [ ] **准备测试音频**：至少 10-20 条，覆盖 7 种情绪
- [ ] **运行情绪模型对比实验**：验证 Emotion2Vec+ 在你的场景下的表现
- [ ] **决策**：Emotion2Vec+ 是否满足需求？需要尝试其他模型吗？

### 短期任务（本周）

- [ ] **实现 MemobaseManager**：`src/context_management/memobase_client.py`
- [ ] **实现 EmotionMemobase**：`src/context_management/emotion_memobase.py`
- [ ] **创建 SQLite 表结构**：`scripts/init_sqlite_schema.sql`
- [ ] **集成到对话流程**：修改 `SpeechTurnProcessor`
- [ ] **端到端测试**：完整对话 → SQLite 存储 → Memobase 处理 → 上下文生成

### 中期优化（下周）

- [ ] **Memobase 本地部署**：Docker 或直接部署
- [ ] **性能测试**：对话延迟是否在可接受范围（< 3秒）
- [ ] **跨会话测试**：验证长期记忆是否生效
- [ ] **多用户测试**：不同年龄段的真实用户测试

---

## 六、常见问题

### Q1：没有真实音频怎么办？

**A**：可以先用模拟数据验证代码逻辑，但最终必须用真实音频测试，因为：
- 模拟数据无法反映真实的情绪特征
- 老年人、中年人、年轻人的语音特点差异很大
- 不同情绪的韵律、语速、音色变化需要真实数据验证

**建议**：录制 10 条测试音频（每种情绪 1-2 条），足够验证系统可用性。

### Q2：Memobase 云端服务和本地部署如何选择？

**A**：本方案只使用本地 Docker 部署，确保患者数据不出域。宿主机服务连接 `http://127.0.0.1:8019`；完整 Compose 启动时语音容器连接固定的 `http://memobase-server-api:8000`，不使用 Memobase 云端服务。

### Q3：Emotion2Vec+ 模型太大（400MB）怎么办？

**A**：
- 如果 GPU 可用：延迟 < 200ms，可接受
- 如果只有 CPU：延迟可能 > 500ms，考虑：
  1. 异步处理：不阻塞对话流程
  2. 降级方案：纯文本情绪识别（已有）
  3. 轻量模型：Wav2Vec2-Emotion-CN（需要对比实验验证）

### Q4：如何验证 Memobase 确实在工作？

**A**：
```python
# 测试脚本
user = memobase_mgr.get_or_create_user("test_user_001")

# 插入几轮对话
for i in range(5):
    chat_blob = ChatBlob(...)
    user.insert(chat_blob, sync=False)

# 触发处理
user.flush(blob_type='chat', sync=True)

# 查看生成的 Profile 和 Event
profiles = user.profile(max_token_size=1000)
events = user.event(topk=10)

print("患者画像:", profiles)
print("关键事件:", events)
```

---

## 七、项目成功标准

### 技术指标

- [ ] 情绪识别准确率 > 70%
- [ ] 对话响应延迟 < 3 秒（含情绪识别）
- [ ] 跨会话记忆有效性：能回忆 3 天前的对话内容
- [ ] 系统稳定性：连续运行 100 轮对话无崩溃

### 业务指标

- [ ] 老年人 AD 筛查：能通过对话初步评估 MMSE
- [ ] 中年人压力管理：能识别压力源并给出建议
- [ ] 年轻人情绪支持：能提供共情陪伴和资源推荐
- [ ] 用户满意度：测试用户反馈"系统能记住我说过的话"

---

## 八、参考资源

- **Memobase 官方文档**：https://github.com/memobase/memobase
- **Emotion2Vec+ 论文**：https://arxiv.org/abs/2312.15185
- **设计文档**：`Memobase集成方案-心理健康对话系统.md`
- **对比实验脚本**：`scripts/benchmark_emotion_models.py`

---

## 九、联系与支持

如果遇到问题：
1. 检查 `voice_server.py` 启动日志
2. 查看 SQLite 数据是否正常写入：`sqlite3 data/patient_memory.db "SELECT COUNT(*) FROM turns;"`
3. 验证 Memobase 连接：`curl http://localhost:8019/health` 或 `curl https://api.memobase.dev/health`
4. 运行单元测试：`pytest tests/test_emotion_memobase.py`

---

**准备好了吗？从准备测试音频开始！** 🎤
