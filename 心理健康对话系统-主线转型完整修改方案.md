# 心理健康对话系统主线转型完整修改方案

> 状态：Emotion2Vec 真实语音情绪识别已修复并完成当前容器验收；默认产品从 AD/MMSE 筛查转为心理健康陪伴仍待按本方案实施。
>
> 本文已按责任边界拆分为“后端修改方案”和“前端修改方案”。共同目标、正式数据流和已完成修复置于前面；后续章节不把待实施内容写成线上现状。

## 1. 转型目标、边界与已确认决策

### 1.1 转型目标

默认产品不再围绕“答题、得分、认知结论”，而是帮助用户表达近况、获得及时而克制的支持，并在数据所有权边界内保持跨会话连续性。系统必须让当前用户能够看懂本轮发生了什么，而不是只留下内部日志。

目标产品的默认会话模式为 `wellbeing`（心理健康陪伴）。AD/MMSE 不删除，但仅作为同一客户端中由用户明确选择的 `cognitive_screening`（认知筛查）专项，不能影响默认陪伴会话。系统只维护一套客户端。

### 1.2 已确认的产品决策

1. 默认主线是心理健康陪伴；AD/MMSE 只保留为同一客户端中由用户明确启动的独立专项。
2. 当前用户可以查看、编辑和删除自己的长期记忆；不新增角色分端和两套页面。
3. 演示使用清楚标记为 `DEMO` 的“工作压力与睡眠困扰”虚构患者，不读取或污染真实患者数据。
4. 患者语音输入后，ASR 与 Emotion2Vec 语音情绪识别并行执行。Emotion2Vec 直接使用音频，不能表述为“根据文字做语音情绪识别”。
5. ASR 可流式产生文本；明显痛苦的文字线索需要及时支持，不能等待整段音频情绪分析完成，也不能直接诊断为抑郁症或其他疾病。
6. 最终整段 Emotion2Vec 结果、长期记忆写入和可解释性事件都不能阻塞 Agent 首句或 TTS 首帧。
7. 用户开始下一轮说话后，上一轮尚未播完的 TTS 必须取消；会话只在用户明确结束或连接断开时结束，不能因静默自动结束。
8. 不使用前端假进度、静默掩盖或伪装多模态结果来掩盖链路异常。

### 1.3 非目标

- 不把 Emotion2Vec 的情绪线索当作精神疾病诊断、治疗建议或紧急服务替代品。
- 不把三层记忆的全文塞入 Agent 上下文，也不强制 Agent 每轮引用旧事。
- 不只隐藏 AD/MMSE 页面元素而让后端继续评分、写认知状态或发送评分事件。
- 不在本次文档重组中提前修改默认会话主线、数据库或页面代码。

## 2. 已完成的 Emotion2Vec 前置修复

下表内容已经完成，和后续心理健康主线改造分开验收。

| 项目 | 已完成事实 | 验证证据 |
| --- | --- | --- |
| 最小运行时 | 在当前容器内验证 `torch==2.4.1+cpu`、`torchaudio==2.4.1+cpu`、`funasr==1.0.27`。 | 本地模型可真实加载并对 WAV 推理。 |
| 本地模型与设备 | 使用容器内 `/app/data/modelscope/models/iic/emotion2vec_plus_large`，CPU 推理并在服务启动后后台预热。 | 真实模型加载约 18 秒；预热后模型可用。 |
| 结果来源 | 真实声学推理成功时才写 `emotion2vec_audio+text`；未用到音频时写 `text_asr`、`text_fallback_model_unavailable`、`text_fallback_audio_missing` 或 `text_fallback_audio_error`。 | `audio_model_used`、`analysis_status` 与本次结果一同传递，避免并发串轮。 |
| 记忆回写 | 初始记录为 `text_asr / provisional`，最终音频融合完成后回写同一回合为 `final`。 | 不再仅因存在 WAV 文件就伪称 `multimodal`。 |
| 回归 | 情绪、记忆、语音服务和处理器相关测试已通过。 | `136 passed`。 |

当前容器已完成真实验收，但 Docker 镜像的重新构建曾受外部 Debian 源 `502` 与镜像代理 EOF 影响。源码中的 Docker/Compose 依赖声明已保存；在一次完整镜像重建并再次运行真实 WAV 验收前，不能把“可从零重建部署”标为已完成。

## 3. 跨端总体架构与正式数据流

### 3.1 一轮语音的正式路径

```text
患者说话语音
    |
    +-- 并行支路 A：流式 ASR
    |      -> 稳定的局部文字
    |      -> 文字情绪线索、显著痛苦文字支持、候选记忆预取
    |      -> 最终 ASR 文本（本轮正式事实输入）
    |
    +-- 并行支路 B：Emotion2Vec 语音情绪
           -> 可用时对实时音频窗口产生可靠的声学摘要
           -> 对完整 WAV 进行最终音频推理和文本融合

最终 ASR 文本
    -> 风险门
    -> 受预算约束的记忆检索
    -> 陪伴 Agent 上下文
    -> 流式文本回复
    -> TTS 首帧与持续播放

异步补全：最终 Emotion2Vec + 情绪轨迹/长期记忆回写
    -> 同一 turn_id 的 turn_insight(final)
    -> 统一客户端“本次记录”与分析详情更新
```

候选记忆检索可以在 ASR 已经稳定的片段上预取，但正式写入、风险判断和 Agent 本轮事实输入以最终 ASR 文本为准。这样既利用流式识别缩短等待，也避免把后来被修正的局部转写持久化为患者事实。

### 3.2 各支路的职责和关键路径

| 支路/阶段 | 输入 | 输出 | 是否允许阻塞首句回复 |
| --- | --- | --- | --- |
| 流式 ASR | 语音帧 | 局部文字、最终文字 | 最终文字是正常等待条件；局部文字只用于预取和实时支持。 |
| 文字线索与风险预判 | 稳定局部文字/最终文字 | 安抚优先级、风险复核请求 | 不等待 Emotion2Vec。 |
| 记忆检索 | 最终文字、受控上下文 | 少量相关旧事和结构化记忆卡 | 只在固定时间预算内等待，超出预算必须记录而非无限卡住。 |
| Agent | 最终文本、短期上下文、会话摘要、少量长期记忆、情绪/风险摘要 | 流式回复 | 是首句关键路径。 |
| TTS | Agent 流式文本 | 首帧和音频流 | 是首段语音关键路径。 |
| 最终 Emotion2Vec | 完整 WAV、最终文本 | 最终情绪来源、分数和轨迹 | 否；完成后更新本轮证据和后续上下文。 |
| 记忆确认写入 | 回合、摘要、最终情绪 | 持久化记忆、`turn_insight(final)` | 否。 |

若实时声学窗口已经在记忆检索预算内产生可靠结果，可作为一个低权重“语音线索摘要”进入本轮 Agent；若尚未完成，Agent 只使用文字和已有上下文。完整音频的最终结果不能倒逼已经开始生成的首句重新等待。

### 3.3 安全检查的含义

| 检查 | 触发依据 | 必须动作 | 不允许的行为 |
| --- | --- | --- | --- |
| 文字情绪支持 | “我真的很难过”“什么都没意思”等明确痛苦、无助、绝望、焦虑或愤怒表达。 | 及时确认感受，温和了解患者当下需要和安全状况。 | 等待声学模型后才回应；把线索直接称为某种疾病。 |
| 最终风险门 | 高风险文字、上下文风险模式及规则化复核。 | 固定安全处置、审计和受限 Agent 策略优先。 | 让通用聊天模型单独决定是否处理。 |
| Agent 输出约束 | 生成中的回复。 | 拦截/改写诊断、危险建议、过度承诺和替代紧急服务的内容。 | 把模型生成文本直接作为安全结论。 |
| 数据治理 | 记忆读取、删除、展示和内部证据边界。 | 当前用户所有权、审计和最小化披露。 | 向客户端发送内部提示词、原始音频路径或不应展示的原始证据。 |

# 第一部分：后端修改方案

## 4. 会话模式、身份归属与 AD/MMSE 隔离

### 4.1 双模式模型

| 属性 | `wellbeing` 默认模式 | `cognitive_screening` 可选专项 |
| --- | --- | --- |
| 创建方式 | 用户正常进入客户端即创建。 | 用户在同一客户端明确选择后创建。 |
| Agent 策略 | 倾听、澄清、支持、小步骤和风险守护。 | 保留现有 MMSE/认知任务流。 |
| 评分和认知状态 | 禁止创建、更新或发送。 | 允许按专项规则处理。 |
| 客户端呈现 | 不显示认知题、得分、教育年限阈值或筛查报告。 | 在同一页面明确显示专项名称、过程和结果边界。 |
| 历史使用 | 只使用当前用户授权的心理健康相关记忆。 | 与默认陪伴历史隔离；不能污染默认 Agent 上下文。 |

数据库会话实体新增不可在会话中途无审计切换的 `mode`。新建默认值为 `wellbeing`；专项创建必须来自同一客户端的明确操作。后端只校验当前用户的会话归属和数据所有权，不新增角色体系。已有 AD/MMSE 历史保持只读，迁移为 `cognitive_screening` 或 `legacy` 标识，不进入默认陪伴上下文。

### 4.2 默认模式必须不存在的 AD 运行路径

在 `wellbeing` 下，以下内容必须在后端被阻断，而不是只在页面隐藏：

```text
MMSEScoringTool
_send_score_update()
update_score WebSocket 事件
MMSE 总分写入
认知状态写入
题图、教育年限阈值和筛查报告生成
```

模式守卫放在会话创建、回合处理、Agent 工具装配和会话结束四个位置。单点 `if` 不足以保证隔离，因为文字回合、语音回合和会话恢复目前都有独立调用路径。

## 5. 语音回合、情绪理解、风险门与 Agent 编排

### 5.1 回合状态和幂等性

每个接受的患者回合生成不可变 `turn_id`。同一 `(patient_id, session_id, turn_id)` 最多对应一条原始轮次、一条最终情绪轨迹和一组可更新的可解释性事件。

```text
CAPTURING
  -> FINAL_ASR
  -> DURABLY_CAPTURED (text_asr / provisional)
  -> SAFETY_GATED
  -> GENERATING
  -> RESPONDED | RESPONSE_CANCELLED | FAILED

异步情绪：
text_asr / provisional
  -> emotion2vec_audio+text / final
或 text_fallback_* / final
```

`text_fallback_*` 只表示当前代码实际返回了文本侧结果，必须如实记录；它不能被显示为“语音情绪识别完成”，也不应通过前端假状态掩盖声学链路故障。本次主线转型不新增静默自动降级的语音回复路径。

### 5.2 陪伴 Agent 策略与上下文

`wellbeing` 的默认优先级如下：

1. 先反映患者已表达的感受或事实，不急于给建议。
2. 情绪明显时先安抚，再询问患者更希望倾诉、一起梳理，还是尝试一个小步骤。
3. 只有在相关且自然时引用记忆，且使用可纠正的措辞，例如“你之前提到过……，如果我记错了请告诉我”。
4. 面对睡眠、工作压力、人际关系等议题，可共同探索可执行的小步骤；不能作治疗承诺或替代专业诊疗。
5. 最终风险门命中时，固定安全处置优先于普通陪伴；Agent 只在受限策略内补充支持。

Agent 每轮只接收下列受控上下文，而不是三层记忆全文：

| 上下文块 | 上限 | 用途 |
| --- | ---: | --- |
| 当前患者最终文字 | 1 条完整文本 | 本轮正式事实输入。 |
| 当前会话短期记忆 | 最近 4 至 8 个相关回合 | 保持连贯、避免重复追问。 |
| 会话摘要 | 1 段，约 300 至 500 字 | 保存主题、情绪变化、已尝试方式和未完成议题。 |
| 长期记忆卡 | 1 张结构化卡 | 稳定偏好、已确认事实和有效安抚方式。 |
| 相关长期证据 | 最多 3 条，约 500 至 800 字 | 只提供与当前话题相关的旧事。 |
| 情绪与风险摘要 | 1 个结构化对象 | 调整语气和触发安全流程；不产生诊断。 |

没有相关记忆时不注入、不引用。患者开始下一轮说话后，后端按 `turn_id` 取消上一轮未完成的 Agent/TTS 流，前端也不能继续播放或追加过期文本。

### 5.3 会话结束规则

用户明确结束时，后端先发送/播放“我们这次就先聊到这里”，随后在后台封存会话摘要、完成记忆整理。连接断开时不再尝试播放，静默封存；用户下次开口创建新会话。静默时间不能自动结束会话。

## 6. 三层记忆、用户控制与数据模型

### 6.1 三层记忆的正式定义

| 层级 | 保存内容 | 生命周期 | 写入规则 | Agent 使用方式 |
| --- | --- | --- | --- |
| 短期记忆 | 当前会话最近相关回合、未播完状态和当轮最终文字。 | 当前会话。 | 每轮前后更新。 | 维持连贯性。 |
| 会话记忆 | 本会话主题、情绪变化、已尝试方式和未完成议题的增量摘要。 | 会话结束前持续更新，结束后封存。 | 仅提升经多轮确认或确实有后续价值的信息。 | 下次会话作为工作背景，不替代长期事实。 |
| 长期记忆 | 结构化记忆卡、详细轮次证据、情绪轨迹和检索索引。 | 跨会话，直到患者删除或数据策略到期。 | 稳定事实、重要事件、偏好、有效安抚方式和可追溯证据。 | 先读权威卡，再检索少量相关证据。 |

SQLite 是长期记忆的唯一事实源。Memobase 只保存可重建的语义检索副本；Memobase 不可用时，系统只能使用 SQLite 权威卡和受控回退查询，不能把索引缺失当作患者没有历史。

### 6.2 长期记忆对象和删除语义

长期记忆项目至少具备 `item_id`、`patient_id`、`mode`、内容、来源类别、关联会话/回合、版本号、删除状态和更新时间。原始音频路径不作为前端可见字段。

患者删除一条长期记忆时，后端必须按以下顺序执行：

1. 验证患者身份、对象所有权、版本号和防重放令牌。
2. 从 SQLite 权威卡和其直接派生摘要中移除或失效该对象。
3. 立即用权威删除状态阻断 Agent 查询；同时提交 Memobase 索引删除任务。
4. 保留不含原文的审计事件，例如“某患者于某时删除 1 项记忆”。
5. 任何缓存、旧索引或页面再次访问时都必须返回“用户已删除”，不能继续将内容注入 Agent。

编辑使用乐观并发控制：客户端提交对象版本；版本冲突返回可处理的冲突结果，不能静默覆盖其他编辑。法定保留例外若未来确有需要，必须在采集前另行说明，不能事后改变“删除”的含义。

## 7. 后端 API 与 WebSocket 契约

### 7.1 记忆 API

在现有 `MemoryApiController` 的读取和更新能力上，补齐按项目的所有权和删除语义。路径暂沿用现有的 `{patient_id}` 数据字段时，只表示内部兼容命名，不表示存在多角色产品：

| 接口 | 用途 | 必须校验 |
| --- | --- | --- |
| `GET /api/memory/{patient_id}/items` | 当前用户查看自己的长期记忆。 | 当前会话身份、数据归属、已删除项过滤。 |
| `PATCH /api/memory/{patient_id}/items/{item_id}` | 当前用户修正可编辑内容。 | 所有权、版本、字段白名单、审计。 |
| `DELETE /api/memory/{patient_id}/items/{item_id}` | 当前用户删除长期记忆项目。 | 所有权、版本、防重放、级联失效和审计。 |

客户端不能提交底层表名、SQL 条件、任意患者 ID 或索引 ID 来决定删除范围。

### 7.2 `turn_insight` 事件

WebSocket 新增结构化 `turn_insight`。初步和最终事件以同一 `turn_id` 更新，前端不解析服务端日志。

```json
{
  "type": "turn_insight",
  "turn_id": "turn-...",
  "state": "final",
  "mode": "wellbeing",
  "emotion": {
    "source": "emotion2vec_audio+text",
    "analysis_status": "final",
    "audio_model_used": true,
    "dominant": "anxiety",
    "scores": {
      "joy": 0.0,
      "sadness": 0.0,
      "anger": 0.0,
      "fear": 0.0,
      "anxiety": 0.0,
      "calm": 0.0,
      "confusion": 0.0
    },
    "inference_ms": 0
  },
  "memory": {
    "used_item_ids": ["..."],
    "written_item_ids": ["..."],
    "summary_changed": false
  },
  "risk": {
    "level": "low",
    "handled": false
  }
}
```

统一客户端只接收当前用户可见的摘要、来源状态和可操作项目 ID；内部证据 ID、规则版本和处置细节按展示白名单裁剪。客户端不接收内部提示词、原始音频路径或不应展示的原始旧事。

## 8. 后端文件级改造清单

| 范围 | 主要文件 | 必须修改内容 | 验证方式 |
| --- | --- | --- | --- |
| 会话模式 | `src/voice/application.py`、会话模型、`src/db/database.py`、迁移脚本 | 创建时写 `mode`；默认 `wellbeing`；同一客户端明确操作后才可创建 `cognitive_screening`。 | 默认会话不能路由到筛查逻辑。 |
| 语音回合 | `src/voice/handlers/speech_turn_processor.py`、`speech_turn_context.py` | 透传模式、文字即时支持、真实实时情绪摘要、最终 `turn_insight`；默认模式不评分。 | 语音路径不再发送 `update_score`。 |
| 文字回合 | `src/voice/handlers/text_turn_handler.py` | 同样按模式隔离 MMSE，写入文字情绪和回合洞察。 | 纯文字 `wellbeing` 不创建认知结果。 |
| 会话生命周期 | `src/voice/handlers/session_lifecycle_handler.py`、`session_resume_handler.py` | 明确结束/断连时封存记忆；只有专项写 MMSE 总分和认知状态。 | 正常陪伴结束后 MMSE 字段无新增写入。 |
| 打断与回复取消 | `voice_interruption_controller.py`、`answer_completion.py`、回复输出处理器 | 新回合启动时取消旧 `turn_id` 的 Agent/TTS 流，不影响新回合记录。 | 新语音开始后旧音频停止且不会追加过期文本。 |
| Agent | `src/agents/screening_agent_function_calling.py`、现有 `src/agents/screening/*` 任务入口 | 按模式装配策略和工具白名单；默认陪伴不加载 MMSE 工具，专项保留旧路径。 | 四类黄金对话可区分。 |
| 风险与实时陪伴 | `src/voice/safety.py`、`realtime_companion.py` | 文字痛苦线索与高风险规则独立于声学模型；受限回复和审计。 | 平静声学结果不能覆盖高风险文字。 |
| 记忆服务 | `src/voice/services.py`、`src/context_management/emotion_memobase.py` | 记忆项目 ID、模式过滤、结构化摘要、删除级联和检索长度预算。 | 删除后不再检索或注入。 |
| 记忆 API/身份 | `src/web/memory_api.py`、`src/web/auth.py`、`src/web/application.py` | 读取/更新/删除路由、当前用户所有权、版本冲突和审计；不新增角色授权。 | 其他用户不可访问；删除后不可读。 |
| 情绪模块 | `src/tools/emotion/*`、`voice_server.py` | 保持已完成的运行时、预热和来源证明；补发最终 `turn_insight`。 | 真实 WAV 与失败来源均如实可追溯。 |
| 部署 | `Dockerfile`、`docker-compose.yml`、`.env` | 保持已完成的最小依赖、本地模型和 CPU 配置；加入发布开关和演示数据开关。 | 完整重建后再次通过真实模型验收。 |

## 9. 后端迁移、回滚、测试与性能验收

### 9.1 数据迁移

1. 迁移前备份 SQLite，导出表行数和当前 schema 版本。
2. 增加 `sessions.mode`、长期记忆对象 ID、删除状态、版本和审计表；历史会话标记为 `legacy` 或依据业务字段标为专项。
3. AD/MMSE 历史只读保留，不进入默认陪伴 Agent 上下文；旧情绪记录若无法证明声学来源，标为 `legacy_unknown`，不能重写成 `emotion2vec_audio+text`。
4. 先以只读兼容模式验证旧数据可见且默认会话不读取 MMSE；再启用 `wellbeing` 默认模式和同一客户端的可选专项入口。
5. 稳定期后才清理只被旧用户页使用的 UI/代码；保留迁移回滚说明和数据备份校验。

建议发布开关：`DEFAULT_SESSION_MODE=wellbeing`、`ENABLE_COGNITIVE_SCREENING=true`、`ENABLE_TURN_INSIGHT=true`、`ENABLE_PATIENT_MEMORY_DELETE=true`。开关仅用于发布和回滚，不能让同一会话的模式或历史语义无审计切换。

### 9.2 后端功能测试矩阵

| 场景 | 必须看到的事实 |
| --- | --- |
| 一般倾诉 | Agent 倾听和澄清；没有 MMSE 问题、得分或认知状态写入。 |
| “我真的很难过” | 文字线索即时安抚；不诊断为抑郁症。 |
| “我感觉什么都没意思” | 温和追问、风险门复核和合适支持；不因声学结果平静而忽略。 |
| 高风险表达 | 固定安全处置、审计、受限 Agent；Agent 故障也不能跳过。 |
| 有长期记忆的重入会话 | 仅在相关时自然引用 1 条旧事，且引用内容可追溯。 |
| 患者删除记忆 | 数据库、Agent 上下文、语义检索和缓存均不再返回该内容。 |
| 可选认知专项 | 在同一客户端明确创建后才出现 MMSE；退出专项后默认仍是 `wellbeing`。 |
| Emotion2Vec 异常 | 来源明确显示实际状态，绝不伪装成多模态或用假进度掩盖。 |

### 9.3 性能观测

每个回合记录下列时间点，并按 p50/p95 报告，不能用估算值替代实测：

```text
T0   患者语音结束
T1   最终 ASR 可用
T2   最终风险门完成
T3   记忆检索完成或达到预算
T4   Agent 首个文本片段
T5   TTS 首帧
T6   最终 Emotion2Vec 完成
T7   最终记忆/turn_insight 回写完成
```

核心约束为 `T4`、`T5` 不等待 `T6`、`T7`。随着长期记忆增加，延迟只能由索引、Top-K、字符上限和检索预算控制，不能通过把全部历史重新拼接到提示词解决。

真实语音验收使用两段情绪标签明确、已获使用许可的 WAV，并传入完全相同的文字：两段音频的原始声学标签/分数和融合前音频分布必须不同，来源均为 `emotion2vec_audio+text`。这只能证明系统真实使用音频，不等于已经完成情绪准确率评估；准确率还需要独立、带真值标注的语料。

# 第二部分：前端修改方案

## 10. 用户端页面与交互

### 10.1 默认用户页

默认页面从“诊室筛查”调整为安静、可重复使用的陪伴对话界面。首屏只呈现会话状态、语音/文字输入、实时转写、对话内容和用户可理解的隐私入口。

必须移除或在默认路径彻底隐藏：MMSE 得分、认知题、教育年限阈值、筛查报告、默认题图和任何暗示用户正在接受 AD 筛查的文案。不是把“得分”改成别的名称，而是默认模式根本不展示这类任务。

用户端需要具备：

| 区域 | 修改要求 |
| --- | --- |
| 会话头部 | 显示“心理健康陪伴”及连接/会话状态；提供“我的记忆”入口和明确结束会话入口。 |
| 输入区 | 保留语音采集、流式文字和文字输入；录音、识别、回复三种状态清楚但不过度装饰。 |
| 对话区 | 显示实时转写、Agent 文本和语音播放状态；新一轮开始时立即停止上一轮残余播放。 |
| 本次记录 | 每轮回复后以可展开区域异步呈现本轮主题、感受线索来源、记忆是否写入和可操作入口。 |
| 错误状态 | 真实显示连接、ASR、TTS 或情绪分析状态；不显示“正在深度分析”等无法验证的假进度。 |
| 会话结束 | 用户主动结束时显示结束状态；断连时不弹出“自动结束”之类的误导文案。 |

### 10.2 情绪和风险的用户可见文案

用户端显示的是支持性、可纠正的“线索”，不是诊断：

| 后端事实 | 用户端建议呈现 | 禁止呈现 |
| --- | --- | --- |
| 文字线索 | “我注意到这段话里有一些难受的感受。” | “你患有抑郁症。” |
| 语音加文字线索 | “这轮的语音和表达都提示你可能有些紧绷；如果我理解错了，请告诉我。” | “语音模型确认你正在焦虑。” |
| 文本回退/声学异常 | “本轮语音情绪分析未完成，系统仅保留了文字线索。” | “语音情绪识别完成。” |
| 高风险处置 | 清楚、平静地提示当前可获得的支持与下一步。 | 把风险等级、规则 ID 或内部判定细节暴露给患者。 |

“我真的很难过”“我感觉什么都没意思”等话语应在对话流中获得及时支持性回复；“本次记录”仅补充解释，不替代当下回应。

## 11. 统一客户端与可选认知专项入口

系统只维护一套客户端，不建立角色页面、路由和状态机。用户进入后默认创建 `wellbeing` 会话；如果确实需要 AD/MMSE，在同一页面通过明确的“认知筛查专项”入口创建新的 `cognitive_screening` 会话。模式在会话创建后固定，不能在一轮对话中途切换。

同一客户端按当前用户的数据所有权展示业务证据，而不是后端调试日志：

| 区块 | 展示内容 |
| --- | --- |
| 会话模式 | `wellbeing` 或 `cognitive_screening`；只有后者显示 MMSE 相关信息。 |
| 情绪线索 | 文字、语音加文字或文本回退来源；主导线索、变化方向和完成状态。 |
| 记忆链 | 参与检索的相关证据、本轮写入的记忆和会话摘要变化。 |
| 风险 | 最终风险等级、处置状态和可理解的支持建议；不显示内部诊断结论。 |
| 用户控制 | 已删除/已编辑状态；删除后不能从缓存、旧页面或旧检索结果恢复内容。 |

这不是“默认页 + 额外角色页面”，而是一套面向当前用户的统一页面。内部调试字段继续留在后端日志或开发诊断接口，不通过新增页面角色解决可解释性问题。

## 12. 前端事件消费与回合状态机

### 12.1 `turn_id` 驱动的显示更新

前端以 `turn_id` 合并 `asr_partial`、最终转写、回复流、TTS 事件和 `turn_insight`，不能用“最新一条事件”覆盖其他回合。最终情绪通常晚于 Agent 首句到达，因此必须更新同一个既有回合的“本次记录”，不能新建一条看似新的对话。

| 前端状态 | 进入事件 | 页面行为 | 退出条件 |
| --- | --- | --- |
| `recording` | 开始采音 | 显示录音与局部转写。 | 用户结束说话或服务端判定回合结束。 |
| `transcribing` | `asr_partial` | 增量更新本轮文字；可触发候选记忆的等待态，但不显示虚构结果。 | 收到最终 ASR。 |
| `replying` | Agent 文本/TTS 流 | 追加同一轮回复并播放音频。 | 回复结束、被打断或失败。 |
| `insight_provisional` | 初步记录/初步事件 | 显示“本轮记录仍在整理”，但仅在后端明确 `provisional` 时显示。 | 收到同一 `turn_id` 的最终事件。 |
| `insight_final` | `turn_insight(final)` | 更新情绪来源、记忆写入和风险摘要。 | 后续仅允许同回合的明确修订。 |
| `cancelled` | 用户开始下一轮/取消确认 | 停止旧 TTS，冻结旧回复，不把旧音频继续播放。 | 新回合独立开始。 |

收到迟到事件时，前端必须先校验 `turn_id`、`session_id` 和状态版本。已经删除、取消或不属于当前用户的项目不能被重新渲染。

### 12.2 可见字段和异常展示

统一客户端只请求当前用户自己的会话和记忆；后端通过会话身份、数据归属和字段白名单保护内部字段，不以角色区分展示。任何未知 `emotion.source`、缺失 `audio_model_used` 或未完成状态都应显示“状态未完成/需确认”，不能默认渲染为“语音加文字”。

前端接到连接、ASR、TTS 或情绪服务故障时，应显示真实的可恢复状态和重新连接操作，但不伪造处理完成、不把失败结果隐藏为中性情绪，也不擅自发起与后端不一致的降级对话。

## 13. 用户长期记忆管理交互

“我的记忆”默认显示人可读卡片，而不是数据库 JSON。每项至少展示内容、来源类别、最近更新时间、关联会话时间和“编辑/删除”操作。

删除流程必须是：选择项目 -> 显示将被移除的内容范围 -> 二次确认 -> 携带版本和防重放令牌调用删除 API -> 从本地列表立即移除 -> 用服务端结果确认。若发生版本冲突、权限变化或删除失败，保留页面内容并提示用户刷新/重试，不能静默覆盖。

被删除的项目在统一客户端显示“已删除”或从默认列表移除。浏览器本地状态、搜索结果和展开的“本次记录”都需要在删除成功后同步清理，避免页面仍能看到已承诺删除的内容。

## 14. 前端文件级改造清单与 UI 验收

当前用户交互主要集中在一个大型静态页面，第一阶段应在现有结构内完成，避免为了改文案和事件接入先引入新的前端框架或无关依赖。

| 范围 | 主要文件 | 必须修改内容 | 验证方式 |
| --- | --- | --- | --- |
| 用户对话页 | `static/voice_chat.html` | 移除默认 MMSE UI；增加陪伴模式、`turn_id` 状态机、本次记录、记忆入口、打断处理和真实错误状态。 | 默认页面无得分/题图；一轮语音能更新同一条记录。 |
| 统一历史/入口页 | `static/history.html`、`static/login.html`、`src/web/application.py` 的静态页路由 | 统一用户的历史、会话模式和记忆入口；不做角色路径分流。 | 同一用户可进入自己的历史和记忆；其他用户数据不可访问。 |
| 旧管理页 | `static/admin.html` | 不作为本次主流程的一部分；如暂时保留，只标为开发/维护页面，不接入演示主链。 | 默认客户端不依赖该页面，直链不改变数据权限。 |
| 认知题资源 | `static/mmse_images/*` | 仅在 `cognitive_screening` 前端路径按需加载，不在默认首屏预加载或引用。 | 默认页面网络请求与 DOM 均无题图。 |
| WebSocket/API 调用 | `static/voice_chat.html` 中现有消息分发和记忆请求 | 消费 `turn_insight`，以 `turn_id` 合并事件；调用记忆读取、更新、删除 API。 | 迟到最终事件不会错配到其他回合。 |

前端验收除功能外还需检查：文本在移动端不溢出；语音输入、转写、回复和错误状态可通过键盘/屏幕阅读器理解；按钮和图标在录音或网络异常时不会重复触发不可恢复操作。

## 15. 前后端联调顺序

| 顺序 | 后端交付 | 前端交付 | 完成判定 |
| --- | --- | --- | --- |
| 1 | 会话 `mode`、当前用户所有权、默认模式的 AD 运行路径阻断。 | 统一入口和模式标识。 | 默认路径没有 AD/UI/事件/数据库副作用。 |
| 2 | 回合状态、风险门、受控 Agent 上下文、旧 TTS 取消。 | 语音回合状态和打断体验。 | 新一轮开始后旧回复停止，文字和语音不串轮。 |
| 3 | 三层记忆对象、检索预算、删除 API 与审计。 | 我的记忆、编辑/删除、冲突提示。 | 删除后后端和页面均无法再读取。 |
| 4 | `turn_insight` 初步/最终事件和用户可见字段裁剪。 | 本次记录和分析详情面板。 | 情绪/记忆/风险均按同一 `turn_id` 真实更新。 |
| 5 | 演示患者数据、性能埋点和回归测试。 | `DEMO` 标识、演示路径和页面验收。 | 3 至 5 分钟演示不依赖 mock 或临场积累。 |
| 6 | 灰度开关、迁移回滚和完整镜像重建验收。 | 旧筛查 UI 清理和发布后回归。 | 切换可回滚，且不发生两种模式混写。 |

## 16. 演示方案：工作压力与睡眠困扰

建立清楚标记为 `DEMO` 的虚构患者，并通过真实后端写入预置两条历史记忆：

| 记忆 | 演示用途 |
| --- | --- |
| 最近项目赶工，常在午夜后才停止工作。 | 展示工作压力的跨会话连续性。 |
| 睡前反复看工作消息会让入睡更困难；愿意尝试把手机放远一点。 | 展示有效应对方式的记忆和患者可删除性。 |

三至五分钟脚本：

1. 打开默认用户页，说明这是陪伴模式，页面没有得分和认知题。
2. 患者说：“最近项目很赶，我已经连续几天睡不好了。”展示流式文字、自然的第一句回应和本轮记录。
3. 患者继续说：“我真的很难过，感觉什么都没意思。”展示文字线索的及时安抚，强调这不是疾病诊断。
4. 打开“本次记录”，展示最终情绪来源、是否真实使用语音模型、写入的会话记忆和相关旧事；不展示内部日志。
5. 明确结束并重新进入。患者说“今天晚上又想一直看工作消息”，系统在相关时自然引用之前的睡眠困扰记忆。
6. 打开“我的记忆”删除一条虚构记忆；新开一轮验证 Agent 不再使用它。
7. 在同一客户端打开模式选择，显式创建一次 `cognitive_screening` 专项，说明 AD/MMSE 被保留但不会污染默认陪伴会话；完成后返回默认 `wellbeing`。

演示中每项可见信息必须来自真实后端写入、检索和事件链；不能用前端静态 mock 伪造记忆、情绪或删除效果。

## 17. 整体验收标准与主要风险

### 17.1 最终验收标准

1. 默认患者会话从 UI、Agent、事件、数据库到生命周期均不再执行 AD/MMSE 逻辑。
2. 同一客户端仍可在明确选择后创建、完成并查看独立的认知筛查专项。
3. ASR 与音频 Emotion2Vec 并行；最终语音情绪的来源、状态和异常原因可追溯。
4. 明显痛苦文字得到及时安抚，高风险文字走确定性安全流程，且二者不被声学模型结果否决。
5. Agent 不接收三层记忆全文，只接收受控、相关、可追溯的上下文。
6. 患者可以查看、编辑、删除自己的长期记忆；删除后不再被检索或生成引用。
7. `turn_insight` 能让当前用户在可见字段范围内理解本轮的情绪、记忆和风险状态；内部调试字段不泄露到客户端。
8. 演示患者可在 3 至 5 分钟内真实展示情绪理解、会话记忆、跨会话记忆和删除效果。
9. 性能报告证明最终 Emotion2Vec 和记忆回写不会增加 Agent 首句或首段 TTS 的关键路径等待。
10. 迁移、回滚、审计和完整镜像重建都有可执行脚本与自动化测试，不依赖人工记忆操作步骤。

### 17.2 主要风险与处理原则

| 风险 | 影响 | 处理原则 |
| --- | --- | --- |
| Emotion2Vec CPU 资源竞争 | 流式 ASR、TTS 或首句回复变慢。 | 启动预热，最终推理异步，监控 CPU/内存及 T4/T5；不伪造结果。 |
| 声学标签被误解 | 被用户或演示者理解为疾病诊断。 | 固定“线索/来源”文案，禁止诊断标签，明确系统不替代专业判断。 |
| 长期记忆过多或不相关 | 提示词膨胀、回复生硬、隐私风险。 | 卡片优先、Top-K、严格长度预算、患者删除和相关性阈值。 |
| 双模式混写 | 陪伴会话仍生成 MMSE 结果。 | 后端模式强约束，测试断言无工具调用、无事件、无数据库写入。 |
| 删除后旧索引命中 | 患者控制权失效。 | SQLite 先删除/失效并立即过滤，索引异步删除前由权威状态阻断。 |
| 外部语音服务异常 | 对话卡住、事件错序或页面显示不真实。 | 分别记录 ASR、TTS、情绪服务故障；先排根因，不用假进度或伪装多模态掩盖。 |
