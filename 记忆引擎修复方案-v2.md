# 记忆引擎修复方案 v2

> 状态：待确认，未实施
> 日期：2026-08-16
> 依据：源码逐函数审查，关键 bug 均有代码行号定位

---

## 1. 核心判断

代码审查后，原方案的问题诊断是准确的，但有三处需要修正或补充：

**修正一：`update_memory_item` 不是 supersede+insert，是原地 UPDATE。**
`emotion_memobase.py:1601-1613` 直接 `SET content=?, version=?`，旧内容被覆盖，证据历史丢失。
原方案说"edit 采用旧 item superseded + 新 item active"，这还不是现状，是目标。实施时需要改写这个函数。

**修正二：Memobase 的 `source_turn_id` 已经在发送。**
`_replay_unsynced_impl:2931` 向 Memobase 发送 `source_turn_id: int(row["id"])`。
原方案把这当作"未验证的外部依赖"，实际已有基础，实施时只需确认搜索结果能回传这个字段。

**补充一：`add_comfort_strategy` 存在特殊的双写漏洞。**
`add_comfort_strategy:1540-1541` 先存 snapshot，再调 `_sync_memory_items_from_snapshot`。
该 sync 函数 `:817` 插入时统一设 `status='active'`，不携带 `effective` 值。
结果是 `get_authoritative_card:2096-2105` 把所有策略都渲染进"有效安抚策略"，包括 `effective=false` 的。
这个漏洞在原方案诊断的 D04 范围内，但根因和修法比原方案描述的更具体。

**架构决定：P3 采用 `RMM-lite 会话级反思 + MemMachine 原始证据保真`。**

- 只吸收 RMM 的会话级“向前反思”：会话结束后异步调用一次 LLM，把整场对话整理为结构化记忆候选和一份会话摘要。
- 不实现完整 RMM 的逐句/逐轮/长期三套摘要，不实现在线强化学习、自动重排和持续重写记忆。
- 借鉴 MemMachine 的核心原则：SQLite 中的原始对话是最终证据；摘要和 memory item 只是便于检索的派生索引，不能替代或改写原始对话。
- LLM 只提出记忆候选和操作建议，服务端完成证据校验、状态判断和数据库写入；LLM 不能直接修改或删除记录。

这样可以获得“结合整场会话理解含义”的收益，同时把调用次数、系统复杂度和模型误判的影响控制在可接受范围内。

---

## 2. 不改的内容

以下三项本轮不动，和原方案一致：

1. 普通轮次写入 Memobase 的时机（仍在明确结束或断连后同步）
2. 文字输入路径的逐轮长期语义检索
3. 患者级记忆同意、分类授权、撤回、保留期和全链路物理擦除

"停止用于陪伴"而非"彻底删除"的文案约束在本轮内有效。

---

## 3. 必须始终成立的不变量

以下规则是可以写成自动化断言的，不是建议：

1. `status` 不为 `active` 的 item（包括 candidate、superseded、deleted、blocked、expired）不能出现在任何传给 LLM 的 prompt 里。
2. 远端 Memobase 命中，如果没有可回查的 `source_turn_id` 对应本地有效记录，不进入 prompt。
3. 同一 `slot_key` + 同一患者，同一时刻最多一个 `active` item。
4. 上下文组装超预算时，按优先级舍弃低优先级内容；不用 `[:N]` 在字符串中间截断。
5. 所有由 LLM 抽取的 item 必须能回查到本地 SQLite 中属于当前患者的原始 user turn。
6. 没有有效 evidence 的候选不得变成 `active`；证据中的引用文字必须真实存在于对应 user turn。
7. 助手回复只能用于理解上下文和指代关系，不能作为“患者说过/发生过”的事实证据。
8. 会话摘要和结构化 item 不能覆盖、替换或删除原始轮次。
9. 系统推断不能静默覆盖用户明确表达；两者冲突时，推断保持 `candidate` 或被 `BLOCK`。

---

## 4. 数据模型变更（最小扩展）

### 4.1 `emotion_memobase_memory_items` 新增字段

只加对修复目标有直接用途的字段，不提前建设未来功能：

| 字段 | 类型 | 用途 | 默认值 |
|---|---|---|---|
| `slot_key` | TEXT NULL | 可替换事实的槽位标识，如 `fact:age`、`fact:address` | NULL |
| `observed_at` | TEXT NULL | 事实发生或被观察的时间，与写入时间分开 | NULL |
| `valid_until` | TEXT NULL | 短期状态的失效时间；NULL 表示长期有效 | NULL |
| `sensitivity` | TEXT | `normal` / `sensitive` / `high` | `'normal'` |
| `confidence` | REAL NULL | 模型对抽取结果的置信度；只用于校准和分流，不能替代证据 | NULL |
| `confirmed_by` | TEXT NULL | 候选由谁确认，如 `user` / `clinician` / `system_rule` | NULL |
| `evidence_json` | TEXT NULL | 受约束的证据数组，保存多个 user turn id 和对应原文片段 | NULL |
| `supersedes_item_id` | TEXT NULL | 明确记录本 item 替代了哪个旧 item | NULL |
| `metadata_json` | TEXT NULL | 仅用于存 `effective`（策略类）、使用次数等少量类别特有属性 | NULL |

保留现有 `source_turn_id` 作为主要证据；`evidence_json` 只在一条记忆需要多个用户轮次共同支持时使用：

```json
[
  {
    "turn_id": "turn-17",
    "quote": "晚上一个人时特别难受"
  },
  {
    "turn_id": "turn-21",
    "quote": "白天有人陪着的时候会好一些"
  }
]
```

服务端必须检查每个 `turn_id` 都属于当前患者和当前会话，并检查 `quote` 确实存在于对应的用户原话中。

现有 `source` 字段收敛为以下枚举，不再新增含义重叠的字段：

```
user_direct        用户当前直接表达
user_correction    用户明确纠正
clinician_confirmed 医护确认
system_inferred    系统/模型推断
legacy_migrated    迁移自旧 snapshot
```

现有 `status` 字段收敛为：

```
candidate    待确认，不注入 prompt
active       当前允许使用
superseded   已被新版本替代，不注入
deleted      已停止使用，不注入（软删除）
blocked      因安全或策略限制禁止注入
```

### 4.2 `emotion_memobase_turns` 新增字段

```
consolidation_status  TEXT  DEFAULT 'pending'   -- pending | consolidated | skipped
consolidated_at       TEXT  NULL
```

用逐轮状态替代患者级水位，是 D01 修复的基础。旧 `last_consolidated_turn_id` 字段保留，降级为迁移兼容和诊断只读字段。

### 4.3 新增表 `emotion_memobase_mirror_ops`

处理 edit/supersede/delete 造成的远端失效，不承担普通轮次同步：

```sql
CREATE TABLE emotion_memobase_mirror_ops (
    op_id           TEXT PRIMARY KEY,
    patient_id      TEXT NOT NULL,
    item_id         TEXT NOT NULL,
    item_version    INTEGER NOT NULL,
    op_type         TEXT NOT NULL,  -- invalidate | delete
    source_turn_id  TEXT,
    status          TEXT NOT NULL DEFAULT 'pending',  -- pending | succeeded | failed
    attempt_count   INTEGER NOT NULL DEFAULT 0,
    next_retry_at   TEXT,
    last_error      TEXT,
    created_at      TEXT NOT NULL,
    updated_at      TEXT NOT NULL
);
```

---

## 5. 改造包 A：确定性一致性（P1 阶段，必须先完成）

### A1. 修复情绪伪分数（D05）

**文件：`src/context_management/emotion_memobase.py`**

`_dominant_emotion:156-164`：全零分或全相等时不返回 `calm`，返回 `"unknown"`。

```python
# 修改前
if highest <= 0 or max(values.values()) - min(values.values()) < 1e-9:
    return "calm"

# 修改后
if highest <= 0 or max(values.values()) - min(values.values()) < 1e-9:
    return "unknown"
```

`_normalise_emotion:198-211`：全零时不生成均匀分布，返回空字典表示"无证据"。

```python
# 修改前
if total <= 0:
    return {name: 1.0 / len(_EMOTIONS) for name in _EMOTIONS}

# 修改后
if total <= 0:
    return {}   # 调用方负责处理空字典，不假设任何情绪
```

所有调用 `_normalise_emotion` 和 `_dominant_emotion` 的地方需检查空返回值：
- `update_emotion_analysis` 写入 trajectory 时，对空分布设 `analysis_status='unavailable'`
- `get_context_for_llm:2280` 处理 `dominant` 时，`unknown` / `unavailable` 的情绪段整体省略，不写"平静"
- `_replay_unsynced_impl:2933` 发送 `dominant_emotion` 给 Memobase 时，`unknown` 原样发送，不替换成 `calm`

### A2. 消除双事实源（D02）

**核心原则：`_sync_memory_items_from_snapshot` 只在迁移路径和 `add_comfort_strategy` 的一处写入时调用，不在读取时调用。**

需要删除以下调用点的 `_sync_memory_items_from_snapshot(...)` 调用：
- `get_authoritative_card:2063`
- `get_context_for_llm:2232`
- `list_memory_items:1554`
- `update_memory_item:1587`
- `delete_memory_item:1643`（同一函数里也有调用）

这些函数改为直接查询 `emotion_memobase_memory_items` 表，不再先从 snapshot 同步。
`_sync_memory_items_from_snapshot` 仅保留为迁移函数。

`update_memory_by_user` 的写路径也同步改为直接操作 items 表，不再更新 snapshot 列表后反向 sync。

### A3. edit 改为 supersede+insert（D02 的一部分）

**文件：`emotion_memobase.py:1568-1620`，函数 `update_memory_item`**

现有实现是原地 `UPDATE content, version`，证据历史丢失。改为：

1. 将旧 item `status` 设为 `superseded`，`updated_at` 更新
2. 插入新 item，内容为修改后的 content，`version=1`，`source=user_correction`，`supersedes_item_id` 指向旧 item 的 `item_id`
3. 如果有 `slot_key`，新 item 继承同一 `slot_key`
4. 两步在同一事务内完成
5. 写入 `mirror_ops` 表，让远端也失效旧 item

旧 item 不删除，保留为不可注入的历史记录。

### A4. 修复乱序沉淀（D01）

**文件：`emotion_memobase.py`，函数 `_pending_consolidation_turns` 和 `consolidate_pending_turns`**

现有 `consolidate_pending_turns:2802` 用 `last_consolidated_turn_id` 作 waterline：

```python
start = int(snapshot.get("last_consolidated_turn_id") or 0)
```

**会话 B 先结束时，waterline 前进；会话 A 的 turn id 落在 waterline 之前，永久跳过。**

修复步骤：

1. 迁移时为 `emotion_memobase_turns` 加 `consolidation_status` 字段
2. `_pending_consolidation_turns` 改为查 `consolidation_status='pending'` 的行，不用 waterline 范围
3. `consolidate_pending_turns` 在事务提交时，把成功处理的行标记 `consolidated`，而不是前移全局水位
4. `CANCELLED`/`FAILED` 的 turn 在读取时直接标记 `skipped`，跳过不报错
5. 抽取或事务失败时，对应 turn 保持 `pending`，下次重试；不能用空 patch 把它标成成功

并发保护：两个 consolidator 并发时，用行级锁（`SELECT ... WHERE consolidation_status='pending' LIMIT N FOR UPDATE` 或 SQLite 的 `BEGIN IMMEDIATE`）防止重复处理同一 turn。

### A5. 修复安抚策略（D04）

**文件：`emotion_memobase.py`，`add_comfort_strategy:1498`，`get_authoritative_card:2096`**

`add_comfort_strategy` 在写入 items 时，把 `effective` 存入 `metadata_json`：

```python
conn.execute(
    f"INSERT OR IGNORE INTO {_MEMORY_ITEM_TABLE} ... metadata_json=? ...",
    (..., json.dumps({"effective": bool(effective)}), ...)
)
```

`get_authoritative_card` 拆分为两段渲染：

```python
effective_strategies = [
    item["content"]
    for item in items_by_category.get("comfort_strategies", [])
    if json.loads(item.get("metadata_json") or "{}").get("effective") is True
]
avoid_strategies = [
    item["content"]
    for item in items_by_category.get("comfort_strategies", [])
    if json.loads(item.get("metadata_json") or "{}").get("effective") is False
]
# effective 未知的不进入任何一段
```

迁移时，已有 comfort_strategies 的 `effective` 从 snapshot 回填；缺失值设为 `None`（不猜 true）。

### A6. edit/delete 立即生效，远端最终一致（D09）

本地提交后，所有读取路径立即过滤旧 item（通过 status 字段，不等 Memobase 成功）。

`_replay_unsynced_impl` 在补发 turns 前，检查该 turn 的 `source_turn_id` 是否在 `mirror_ops` 的失效集合中；若在，跳过该 turn，不补发。

`mirror_ops` 后台重试，指数退避，达到上限后进入 `failed` 状态并记录日志，不恢复本地可见性。

---

## 6. 改造包 B：上下文质量（P2 阶段）

### B1. 预算化卡片构建（D03）

**文件：`emotion_memobase.py:get_authoritative_card`，`wellbeing_companion_agent.py:WellbeingMemoryTool`**

现有问题：
- `get_authoritative_card:2106` 先拼字符串再 `_clip(..., _MAX_CONTEXT_CHARS)` 截断
- `WellbeingMemoryTool.set_persistent_background:30` 的 `[:4000]`
- `WellbeingMemoryTool.get_context:49` 的 `[:6000]`
三处截断各自独立，高优先级内容可能被前面的低优先级内容挤掉。

**修改方案：** 重写 `get_authoritative_card` 的组装逻辑：

1. 先过滤：只保留 `status='active'` 且未过期（`valid_until` 为 NULL 或未到期）的 item
2. 排序：用户当前纠正/边界 > 高置信来源 > 相关度 > 时效 > 新近性
3. 分配预算（初始值，后续用 oracle 校准）：

   | 内容块 | 初始预算（字符） |
   |---|---|
   | 用户当前纠正、边界、应避免方式 | 600 |
   | 基本信息与当前事实 | 650 |
   | 近期事件/触发因素 | 1000 |
   | 偏好与有效安抚策略 | 650 |
   | 情绪趋势摘要 | 300 |
   | narrative 和余量 | 800 |

4. 按预算完整组装，未用完的预算可动态让渡给下一类
5. `WellbeingMemoryTool` 的 `[:4000]` 和 `[:6000]` 两处截断，改为接收已满足预算上限的字符串，不再二次截断

### B2. 情绪趋势进入陪伴上下文（D07）

`get_context_for_llm:2280` 和 `get_authoritative_card` 的情绪段目前展示单点最新情绪，不区分是否有证据。

修改：
- 只取最近 N 个 `analysis_status='final'` 的 trajectory 点（而非所有点）
- 标明时间窗口（最早和最后观测时间）
- 可描述主导情绪变化和反复触发因素
- 没有 final 行时，整个情绪趋势段省略，不写"平静"
- 不输出诊断性结论

### B3. Memobase 命中的本地状态过滤（D09 的补充）

`get_relevant_evidence` 返回 Memobase 结果时，加一步回查：

```python
def _filter_memobase_hit(self, conn, patient_id: str, hit: dict) -> bool:
    source_turn_id = hit.get("source_turn_id")
    if not source_turn_id:
        return False  # 无 provenance，丢弃
    # 检查对应 item 是否仍 active
    # 检查该 turn 是否在失效集合中
    ...
```

这依赖 Memobase 搜索结果返回 `source_turn_id`。`_replay_unsynced_impl:2931` 已经在发送时带了这个字段，需要确认 Memobase SDK 的搜索 API 是否在结果中回传它。**这是 P2 开始前必须手动验证的一项。**

### B4. 本地降级检索（D08）

触发条件仅限 Memobase unavailable/error/timeout，不与正常远端结果混排。

实现：
1. 首先用 `active items` 按关键词/n-gram 评分，最多 3 条
2. 如用户允许，对"已完成且非当前会话"的轮次建 SQLite FTS5 trigram 索引做补充检索

降级结果必须标注来源（`retrieval_source: local_fallback`），检索不到就明确返回空。

**实施前确认：** 目标环境的 SQLite 版本是否 ≥ 3.34 且编译了 trigram tokenizer。

---

## 7. 改造包 C：情感抽取（P3 阶段，在 P1/P2 稳定后）

### C1. RMM-lite 会话级反思

现有 `_extract_memory_items:247` 主要依赖正则，只能稳定抓取姓名、年龄、简单喜好和少数关键词事件。它适合做高精度兜底，但无法理解需要结合多轮对话才能看出的情绪模式、触发因素、边界和有效安抚方式。

P3 改为以下流程：

```text
SQLite 原始轮次
  -> 会话明确结束或断连后，异步调用一次 LLM
  -> 输出结构化 memory candidates + 一份 session_summary
  -> 服务端校验 schema、患者/会话归属、证据原文和冲突
  -> 执行 ADD / SUPERSEDE / CANDIDATE / NOOP / BLOCK
```

限制：

- 不在患者说话或实时回复的关键路径中调用抽取 LLM，不增加实时陪伴延迟。
- 每个会话最多生成一份 `session_summary`，不为每句话生成摘要，也不同时维护轮次、会话、长期三套重复摘要。
- `session_summary` 用于保存整场对话的叙事脉络，不等于已确认事实，不能直接覆盖结构化 item。
- 多轮归纳出的模式默认是 `candidate`。例如“负面情绪和失眠可能集中在夜间”可以作为候选，但不能冒充患者亲口确认的事实。

### C2. MemMachine 原始证据保真

- SQLite 中的原始 user turn 是最终事实来源，结构化 item 和会话摘要都只是派生索引。
- 每条候选必须绑定一个或多个原始 user turn，并保留短的 `evidence_quote`。没有有效证据就不能成为 `active`。
- 助手回复可以帮助模型理解“他”“那件事”等指代，但助手说过的话不能被当成患者事实。
- 修改、确认、停止使用或重新生成 item/summary，都不能修改原始轮次。
- 检索命中 item 后，技术上可以回查同一会话的相邻原始轮次补充语境；是否允许跨会话原文进入 prompt，仍按第 15 节的本地降级范围单独确认。

这条原则的实际作用是：模型总结错了，可以重新生成索引；原始对话还在，系统仍能查清患者当时到底说了什么。

### C3. 受约束的结构化抽取和更新建议（D06）

复用项目现有 LLM provider 抽象（`src/llm/http_client_pool.py`），不引入新依赖。模型必须输出固定 JSON schema，例如：

```json
{
  "session_summary": {
    "content": "用户谈到夜间独处时更容易想起过去经历并出现失眠。",
    "evidence_turn_ids": ["turn-17", "turn-21"]
  },
  "memory_candidates": [
    {
      "category": "emotional_triggers",
      "content": "夜间独处可能加重难受和失眠",
      "source_turn_id": "turn-17",
      "evidence_turn_ids": ["turn-17", "turn-21"],
      "evidence_quote": "晚上一个人时特别难受",
      "observed_at": "2026-08-13",
      "valid_until": null,
      "sensitivity": "sensitive",
      "confidence": 0.86,
      "slot_key": "trigger:night_alone",
      "suggested_operation": "CANDIDATE"
    }
  ]
}
```

第一版只抽取真正影响陪伴回复的类别：`facts`、`preferences`、`events`、`emotional_triggers`、`boundaries`、`comfort_strategies`。

模型只能建议以下操作：

| 建议 | 含义 |
|---|---|
| `ADD` | 新增一条证据充分、没有冲突的记忆 |
| `SUPERSEDE` | 用户明确纠正旧事实，建议用新 item 替代旧 item |
| `CANDIDATE` | 有价值但仍属于推断、敏感、低置信或存在冲突 |
| `NOOP` | 没有值得进入长期记忆的信息，或只是重复已有内容 |
| `BLOCK` | 证据无效、越权、无法归属或不应进入陪伴记忆 |

服务端必须验证：字段和 category 在白名单内、长度符合限制、证据 turn 属于当前患者和当前会话、引用原文真实存在。模型不能拼 SQL、不能直接写数据库、不能建议物理删除；数据库操作由程序根据验证结果执行。

现有规则提取器继续保留：LLM 暂时不可用时用于姓名、年龄等高精度降级抽取，也用于和 LLM 结果交叉检查。规则结果与 LLM 结果冲突时，不自动选择其中一个，新结果进入 `candidate`。

### C4. candidate 与 active 的自动分类规则

| 条件 | 结果 |
|---|---|
| 低敏、用户直接表达、高置信（≥ 阈值）、证据有效、无 slot 冲突 | 直接 `active` |
| 高敏 OR 系统推断 OR 低置信 OR 有冲突 | 保存为 `candidate` |
| 多轮信息归纳出的模式或因果关系 | 保存为 `candidate`，不能伪装成用户确认的事实 |
| 用户当前直接表达的边界，即使 sensitive | 直接 `active`（违反边界的风险高于等待确认） |
| 危机、创伤、诊断、家庭冲突 + 仅凭推断 | 只能 `candidate`，必须由用户或医护确认才能 active |

置信阈值（如 0.85）不能在代码里硬编码，应在抽取 oracle 建立后通过校准确定。在没有评测数据前，代码里用环境变量 `MEMORY_AUTO_ACTIVE_CONFIDENCE_THRESHOLD` 控制，默认保守值 0.9。

### C5. 冲突与时效处理

- 用户明确纠正有 `slot_key` 的当前事实时：旧 item 标记为 `superseded`，新建 item；不原地覆盖。
- 模型推断与已有事实冲突时：推断保持 `candidate` 或 `BLOCK`，不能替换用户明确表达。
- 证据不足以判断新旧关系时：旧 item 保持不变，新项保持 `candidate`。
- `valid_until` 已过的 item 不作为当前状态注入，作为带日期的历史事件保留。
- 历史危机表达只能描述为“某日期曾表达……”，不推断当前仍处于危机。
- `session_summary` 不能单独作为危机状态、诊断或长期事实的激活依据。

### C6. 本轮明确不采用

为控制复杂度，本轮不实现：

- 完整 RMM 的在线强化学习、根据回复效果自动重排或重写记忆
- 每句话一份摘要，以及轮次/会话/长期三套重复摘要
- A-MEM 式自动重写旧记忆
- Zep/Graphiti 图数据库
- MemoryOS 全套分层和多智能体记忆管理
- MemoryBank 式按遗忘曲线自动删除患者重要经历或边界

---

## 8. 改造包 D：用户纠错界面（P4 阶段）

### D1. API 层（`src/web/memory_api.py`）

`MemoryItemUpdateRequest:96` 和 `MemoryItemDeleteRequest:111` 已有 `expected_version` 和 `deletion_token`，API 契约已就绪。

需要补充：
- 增加 candidate confirm/reject 操作（reject 转为 `blocked`，不物理删除）
- list item 返回 `sensitivity`、`observed_at`、`valid_until`、`confirmed_by`、`retrieval_source`；默认不返回完整敏感原文
- "清空 profile 字段"使用显式 null 语义（目前 `_build_notes_payload:55` 跳过 None，导致无法清除字段）

### D2. 前端（`frontend/src/components/MemoryCard.vue`）

- 首次加载保存 revision，整体保存时提交 `expected_revision`；409 时保留用户未提交内容并展示冲突
- 逐步从整块文本框切换为逐 item 编辑
- 每项显示类别、状态标签、更新时间和简短来源
- 操作按钮统一为"纠正""确认候选""停止用于陪伴"，不出现"删除"或"彻底清除"
- 实施后必须做真实页面截图验收

---

## 9. 数据迁移

**策略：只加字段和加表，本轮不删旧字段。**

### M1. schema 扩展
- 备份 SQLite 文件
- 加 items 表新字段（`slot_key`、`observed_at`、`valid_until`、`sensitivity`、`confidence`、`confirmed_by`、`evidence_json`、`supersedes_item_id`、`metadata_json`）
- 加 turns 表新字段（`consolidation_status`、`consolidated_at`）
- 加 `mirror_ops` 表

### M2. 回填
- 现有 `active` items 的 `consolidation_status` 对应轮次推断为 `consolidated`（保守：只有能找到对应 turn 的才标）
- 其他轮次保持 `pending`，由幂等规则处理
- `comfort_strategies` 的 `effective` 从 snapshot 回填到 `metadata_json`；缺失值 → NULL，不猜 true
- 所有旧 items 的 `source` 若为空或 `derived`，保持原值或标为 `legacy_migrated`
- 已存在的 `deleted` / `superseded` item，状态优先保留，迁移不得把 tombstone 复活

### M3. 切读与验证
- 同时生成旧 snapshot 列表视图和新 items 视图，比较差异（而非双写）
- 验证无跨患者差异、无 tombstone 复活、策略有效性一致
- 通过后切换读路径到 items，snapshot 列表改为只读投影

### M4. 稳定后收尾
- 停止正常写路径调用 `_sync_memory_items_from_snapshot`
- 保留兼容字段至少一个发布周期；确认稳定后再单独决定是否删除

---

## 10. 实施顺序

| 阶段 | 内容 | 先决条件 |
|---|---|---|
| P0 | 写缺陷复现测试、构建迁移前基线（患者数、item 数、tombstone 数、策略有效性分布） | 现有 146 项测试全通过 |
| P1 | schema 迁移、逐轮 consolidation_status、消除双事实源、supersede+insert、情绪 unknown、安抚策略有效性分离、mirror_ops | P0 基线建立，D01/D02/D04/D05/D09 零容忍断言通过 |
| P2 | 预算化卡片、情绪趋势、Memobase 过滤（需先确认 source_turn_id 回传）、本地降级 | P1 全部通过，延迟门槛满足，Memobase provenance 手动验证通过 |
| P3 | RMM-lite 会话级反思、结构化候选抽取、原始证据绑定（以 shadow/candidate 模式先运行） | P2 稳定；oracle 标注集 ≥ 50 条；evidence 归属校验通过；敏感推断误激活为 0；active 精度门槛通过 |
| P4 | 逐 item API、前端纠错、candidate confirm/reject | API 并发 409 恢复通过；真实页面截图验收通过 |
| P5 | 小流量开启自动 active；配对盲评 | 安全、隔离、质量门槛全部通过 |

**P1 不依赖 P3**：先把现有有限记忆做正确，再扩大抽取范围。

---

## 11. 测试

### 11.1 确定性正确性（零容忍，P1 阶段必须全通过）

- 会话 A 后结束、会话 B 先结束，A 的所有轮次都被沉淀，无漏记
- 并发两个 consolidator，无重复 item，无遗漏 turn
- 80 岁纠正为 81 岁后，只有 81 进入 prompt，旧 item 为 superseded
- 停止使用后，任何新 prompt 中不出现该 item 的内容
- `effective=false` 的策略只出现在"应避免"段；未知 effective 不出现在任何可执行策略段
- 空分类时，`calm`、均匀概率、非零可用 arousal 均不出现
- candidate/deleted/superseded/blocked/expired 状态的 item 均不注入
- 停止使用的 item 对应的 source turn，在补发时被跳过
- 最新高优先级 item 在总预算内完整保留

### 11.2 检索 oracle

人工标注集覆盖：害怕独处、睡眠诱因、丧失经历、边界、家庭冲突、纪念日反应、有效/无效安抚策略；新旧事实冲突；相似措辞不同患者；历史危机与当前无危机的组合。

指标：Recall@3、关键记忆漏检率、无关注入率、失效事实使用率、跨患者错召回率。

### 11.3 抽取 oracle（P3 用）

逐 item 标注 category、slot、sensitivity、是否应 active。分别统计：自动 active precision；active+candidate 总 recall；冲突识别正确率；高敏推断误激活数；无证据虚构数。

必须覆盖以下用例：

- 助手提出的建议或猜测不能被抽取成患者事实。
- `evidence_quote` 必须真实存在于对应 user turn，且 turn 必须属于当前患者和当前会话。
- 否定表达、过去状态、暂时状态不能被写成长期的当前事实。
- 多轮归纳出的情绪模式、触发关系和因果关系默认进入 `candidate`。
- 原始轮次在 summary/item 被更新、替换或停止使用后仍能完整回查。
- 同一段对话中的事件、触发因素、边界和有效/无效安抚策略能被分别抽取。
- 用户明确纠正时生成 `superseded + 新 item`，模型推断不得覆盖用户明确表达。
- LLM 不可用时，现有规则提取器仍能完成姓名、年龄等高精度降级抽取。

### 11.4 真实服务与故障测试

- Memobase 正常、超时、断开、恢复
- 服务在沉淀提交前/后、远端失效中途重启
- S=1000 数据规模下的卡片构建延迟（p95 目标：关键路径 ≤ 当前语音超时预算）
- API 并发修订和 409 恢复
- 关闭长期记忆时不产生隐式读写

### 11.5 配对盲评（P5 用）

同一组多轮场景分别以 memory-off 和 memory-on 生成回复，隐藏条件后评审从以下维度评分：情感连续性、相关性、事实一致性、边界遵守、冒犯/过度熟悉感、安全性。

**评价的不是"记得多不多"。在情感陪伴中，错误地记得比暂时没想起来风险更高。**

---

## 12. 硬验收门槛

| 维度 | 门槛 |
|---|---|
| 乱序/并发沉淀 | 漏处理、重复生效均为 0 |
| 状态一致性 | 非 active 状态注入次数为 0 |
| 跨患者隔离 | 错召回次数为 0 |
| 用户纠正 | 成功响应后旧值进入新 prompt 的次数为 0 |
| 情绪 unknown | 无证据被解释为 calm 或可用 arousal 的次数为 0 |
| 高敏自动抽取 | 仅推断产生的高敏 active 数为 0 |
| 原始证据绑定 | 无有效 user-turn evidence 的 active item 数为 0；伪造或跨患者引用数为 0 |
| 自动 active 精度（P3） | oracle 上 ≥ 95% |
| 关键记忆 Recall@3 | ≥ 95%（P0 基线后确认；未达标时不开启原文降级以外的扩量） |
| 无关注入率 | ≤ 5%，且不含失效或跨患者内容 |
| 回归 | 现有 146 项全通过，新增测试全通过 |
| 配对盲评 | 安全和边界不劣于 memory-off；连续性改善且事实错误不增加 |

确定性正确性门槛（前六项）不可因样本不足而降低；质量指标（后几项）在 P0 基线后由你确认。

---

## 13. 失败处理与回滚

- 抽取 LLM 临时失败：turn 保持 pending，记录错误类型并稍后重试；不阻断会话结束。达到重试上限后才运行现有高精度规则提取器降级；规则也失败时继续保留 pending，不用空结果冒充成功
- Memobase 失败：本地权威卡和结构化 items 继续工作；本地降级是否开启取决于你的确认
- revision 冲突：返回 409，最多重试 3 次；持续冲突则保留 pending，不前移水位
- mirror op 长期失败：本地持续过滤，队列显示 failed 并记录日志

回滚：schema 只加字段不删字段，切读前有 item→snapshot 兼容投影。情感抽取、本地降级、V2 读取各有独立开关。回滚时已标记 deleted/superseded 的 item 不得重新投影为 active。

---

## 14. 可观测性（不记录记忆正文）

- pending/consolidated/skipped 轮次计数
- revision 冲突和沉淀重试次数
- candidate/active/superseded/deleted item 数
- Memobase hit 总数、被本地状态过滤的 hit 数、本地 fallback 次数
- mirror op backlog、重试次数、失败数
- 各类别预算入选/舍弃字符数和卡片构建耗时

---

## 15. 实施前必须你来确认的四项

1. **唯一事实源**：是否接受 `memory_items` 成为结构化记忆的唯一权威来源，snapshot 列表只作迁移兼容投影？

2. **candidate 自动启用规则**：是否接受"低敏、用户直接表达、高置信、无冲突"直接 active；高敏、低置信、系统推断、冲突保持 candidate？用户直接表达的边界例外立即 active。

3. **本地降级范围**：Memobase 故障时，只召回结构化 active items，还是允许同时召回经过患者/会话/状态/时效过滤的跨会话原文？

4. **Memobase provenance 验证**：在进入 P2 之前，需要手动调用一次 Memobase 搜索，确认结果中包含 `source_turn_id` 字段。如果不包含，P2 的远端过滤逻辑需要调整为保守策略（不用 Memobase 结果做个性化，只用结构化 items）。

`RMM-lite 会话级反思 + MemMachine 原始证据保真` 已在本轮确认，不再列为待确认项。

未确认以上四项前，本方案停留在设计阶段，不修改业务代码。

---

## 16. 研究依据（借鉴原则，不照搬整套系统）

- [RMM: Reflective Memory Management for Long-term Personalized Dialogue Agents（ACL 2025）](https://aclanthology.org/2025.acl-long.413/)：借鉴会话级反思；不采用在线强化学习和完整多粒度记忆重组。
- [MemMachine（2026）](https://arxiv.org/abs/2604.04853)：借鉴原始 episodic memory 作为 ground truth，摘要和索引不得替代原始证据。
- [Mem0（2025）](https://arxiv.org/abs/2504.19413)：借鉴“先抽取、再与已有记忆对齐并决定新增/更新/忽略”的两阶段处理。
- [LongMemEval（2024）](https://arxiv.org/abs/2410.10813/) 与 [LoCoMo（2024）](https://arxiv.org/abs/2402.17753/)：用于设计长期记忆的检索、时间、冲突和多轮连续性评测。
